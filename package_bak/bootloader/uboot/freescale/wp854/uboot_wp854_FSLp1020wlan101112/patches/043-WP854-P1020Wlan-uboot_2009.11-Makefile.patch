--- u-boot-freescale-p1020-sku/Makefile	2012-07-11 19:41:34.467329687 -0700
+++ u-boot-liteon-wp854-openwrt/Makefile	2011-07-28 20:51:27.492849000 -0700
@@ -21,15 +21,23 @@
 # MA 02111-1307 USA
 #
 
-VERSION = 2009
-PATCHLEVEL = 11
+VERSION = 1.0
+PATCHLEVEL = 7
 SUBLEVEL =
 EXTRAVERSION =
-ifneq "$(SUBLEVEL)" ""
-U_BOOT_VERSION = $(VERSION).$(PATCHLEVEL).$(SUBLEVEL)$(EXTRAVERSION)
+
+ifneq "${BOOT_VERSION}" ""
+U_BOOT_VERSION = $(BOOT_VERSION)
 else
-U_BOOT_VERSION = $(VERSION).$(PATCHLEVEL)$(EXTRAVERSION)
+U_BOOT_VERSION = $(VERSION).$(PATCHLEVEL)
 endif
+
+#ifneq "$(SUBLEVEL)" ""
+#U_BOOT_VERSION = $(VERSION).$(PATCHLEVEL).$(SUBLEVEL)
+#else
+#U_BOOT_VERSION = $(VERSION).$(PATCHLEVEL)
+#endif
+
 TIMESTAMP_FILE = $(obj)include/timestamp_autogenerated.h
 VERSION_FILE = $(obj)include/version_autogenerated.h
 
@@ -382,8 +390,7 @@
 		cat $(ONENAND_BIN) $(obj)u-boot.bin > $(obj)u-boot-onenand.bin
 
 $(VERSION_FILE):
-		@( printf '#define U_BOOT_VERSION "U-Boot %s%s"\n' "$(U_BOOT_VERSION)" \
-		 '$(shell $(TOPDIR)/tools/setlocalversion $(TOPDIR))' ) > $@.tmp
+		@( printf '#define U_BOOT_VERSION "U-Boot %s"\n' "$(U_BOOT_VERSION)" ) > $@.tmp
 		@cmp -s $@ $@.tmp && rm -f $@.tmp || mv -f $@.tmp $@
 
 $(TIMESTAMP_FILE):
@@ -2535,9 +2542,11 @@
 P2020RDB_SPIFLASH_config:	unconfig
 	@$(MKCONFIG) -t $(@:_config=) P1_P2_RDB ppc mpc85xx p1_p2_rdb freescale
 
+P2020SKU_config\
 P1020SKU_config:	unconfig
 	@$(MKCONFIG) -t $(@:_config=) P1020_SKU  ppc mpc85xx p1020_sku freescale
 
+
 PM854_config:	unconfig
 	@$(MKCONFIG) $(@:_config=) ppc mpc85xx pm854
 
